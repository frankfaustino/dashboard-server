type User {
  id: ID!
  firstName: String
  lastName: String
  username: String
  email: String
  token: String
  sessionCount: Int
}

type LoginResponse {
  sessionId: String
  error: String
}

type Query {
  me(sessionId: String): User
  users: [User]
  user(id: ID!): User
}

type Mutation {
  login(username: String!, password: String!, environment: String!): LoginResponse
}

enum Role {
  ADMIN
  USER
}